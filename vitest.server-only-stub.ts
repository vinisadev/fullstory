// Test-only stub for `server-only`. The real module errors when imported in a
// non-server context; in vitest's Node runtime that triggers, so we alias it
// to this empty module via vitest.config.ts.
